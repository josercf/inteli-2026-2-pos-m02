# Aula 02 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir os quatro artefatos públicos, as notas de condução e as decisões arquiteturais da Aula 02 (15/08/2026, "O dado sujo é o case"), com todo número do case travado por teste e a diretiva editorial contra paralelismo travada por validador.

**Architecture:** Os construtores de slide hoje embutidos em `tools/montar_deck_aula01.py` são extraídos para `tools/deck_kit.py` antes de qualquer coisa, com o deck da Aula 01 regerado byte a byte idêntico como prova da extração. O deck da Aula 02 consome o kit. Os números do dia vivem em um único módulo de teste que os transcreve de forma literal a partir do CSV entregue, e nenhum material cita número que não esteja lá. Um validador novo (`check_retorica.py`) trava as três construções retóricas proibidas.

**Tech Stack:** Python 3 em `.venv`, pandas, pytest, Reveal.js já vendorizado em `assets/vendor/reveal`, Playwright para `check_slides.py`. Sem bundler, sem package manager de JavaScript.

**Spec:** `docs/superpowers/specs/2026-08-15-aula02-design.md`

## Global Constraints

Valem para todo arquivo tocado por este plano. Copiados da spec e do `CLAUDE.md`.

- Português do Brasil com **acentuação completa**, inclusive em comentários de código e mensagens de commit de conteúdo.
- **Em dash (U+2014) proibido.** Usar dois-pontos, vírgula, parênteses ou hífen.
- **Emoji proibido** em deck, portal, material, referências e nome de arquivo. A iconografia é o Material Symbols.
- **Paralelismo proibido**, em três formas: paralelismo negativo (`não é X, é Y`), antítese simétrica (duas orações espelhadas contrastando dois objetos) e escalada com dois-pontos (`não apenas X: Y`). Referência de escrita: as apresentações da McKinsey. Título de slide de conteúdo é a conclusão completa com o número dentro; o corpo a sustenta.
- **Título de slide de conteúdo é afirmativo.** Título de prática, quiz e divisor **não** é afirmativo: em quiz, título afirmativo entrega o gabarito.
- **Todo slide de conteúdo fecha com faixa de conclusão**, dentro de `.fecho`, junto com a linha de fonte.
- **Nenhum número do case aparece em material didático sem estar travado por teste.**
- **`aulas/aula01.html` e `aulas/aula02.html` são gerados, nunca editados à mão.**
- **Figura é governada pela largura**, desenhada em 1168px e renderizada 1:1. Nunca `max-height`.
- **`concept-cards` é grade de três colunas.** Para quatro blocos, usar o modificador `quatro`.
- **`recebidos/` e `docs/notas-do-professor/` nunca são commitados.**
- **`tools/build_site.py` monta o Pages por allowlist.** Arquivo novo que vai ao ar entra na lista.
- Commits em Conventional Commits, escopo `aula02`, `dados`, `tools` ou `docs`.
- Comando de teste padrão: `.venv/bin/python -m pytest <caminho> -q`.

### Valores canônicos do dia

Medidos em `dados/kovan_painel_contas.csv` (16.618 linhas, 1.187 contas, 14 trimestres). Transcritos aqui de forma literal, e é daqui que as asserções saem.

| Fato | Valor exato |
|---|---|
| Linhas com `receita_brl` nula | 160 |
| Status dessas linhas | apenas `Ativa` |
| Linhas com `valor_medio_pedido_brl` nulo | 160, as mesmas |
| Dessas, no segmento Estratégico | 14 |
| Linhas sem `visitas_registradas` | 8.280 |
| Linhas sem `interacoes_crm` | 8.280, as mesmas |
| Taxa de ausência de visitas, Estratégico | 0,2942 |
| Taxa de ausência de visitas, Médio | 0,4990 |
| Taxa de ausência de visitas, Cauda | 0,5317 |
| Visitas médias, ausente vira zero | Cauda 2,1012, Médio 2,2959, Estratégico 2,4479 |
| Visitas médias, excluindo ausentes | Estratégico 3,4683, Cauda 4,4864, Médio 4,5827 |
| Ordem crescente, ausente vira zero | Cauda, Médio, Estratégico |
| Ordem crescente, excluindo | Estratégico, Cauda, Médio |
| Estratégico, rompeu contra não rompeu (zero) | 1,5063 contra 2,8291 |
| Estratégico, rompeu contra não rompeu (excl.) | 2,2476 contra 3,9280 |
| `linhas_produto_ativas` média, 2022Q4 | 1,8484 |
| `linhas_produto_ativas` média, 2023Q1 | 3,6698 |
| `linhas_produto_ativas` média, 2025Q4 | 2,5535 |
| `taxonomia_mix == "pre_2023"` | apenas 2022Q3 e 2022Q4 |
| `devolucoes_brl` negativas / zeros / positivas | 15.999 / 619 / **0** |
| Receita bruta total | 8.406.227.803,71 |
| Receita líquida somando | 8.297.527.227,90 |
| Receita líquida subtraindo | 8.514.928.379,52 |
| Diferença entre as duas | 217.401.151,62 |

---

## Estrutura de arquivos

| Arquivo | Responsabilidade | Task |
|---|---|---|
| `dados/tests/test_aula02_numeros.py` | Trava os valores canônicos acima contra o CSV | 1 |
| `tools/check_retorica.py` | Validador das três construções proibidas | 2 |
| `tools/tests/test_check_retorica.py` | Prova que o validador reprova e aprova o que deve | 2 |
| `tools/deck_kit.py` | Construtores de slide, extraídos da Aula 01 | 3 |
| `tools/montar_deck_aula01.py` | Passa a consumir o kit, saída byte a byte idêntica | 3 |
| `tools/gerar_figuras_aula02.py` | Três figuras em 1168px | 4 |
| `tools/tests/test_figuras_aula02.py` | As figuras do deck são as que o gerador produz | 4 |
| `tools/montar_deck_aula02.py` | Gera `aulas/aula02.html` | 5 |
| `tools/tests/test_deck_aula02.py` | Convenções de deck, quiz e retórica | 5 |
| `materiais/skill-limpeza-kovan.md` | Esqueleto que os grupos preenchem na Prática 4 | 6 |
| `tools/tests/test_skill_limpeza.py` | O esqueleto cobre as quatro advertências | 6 |
| `tools/montar_notebook_aula02.py` | Gera `notebooks/aula02_limpeza.ipynb` | 7 |
| `materiais/aula02-material-de-apoio.html` | Aprofundamento escrito | 8 |
| `docs/notas-do-professor/aula02.md` | Condução, não commitado | 9 |
| `docs/adrs/ADR-003-*.md`, `ADR-004-*.md` | Decisões arquiteturais | 10 |
| `PLANEJAMENTO_AULA_A_AULA.md`, `index.html`, `tools/build_site.py` | Amarração e publicação | 10 |

---

### Task 1: Travar os números da Aula 02

**Files:**
- Create: `dados/tests/test_aula02_numeros.py`

**Interfaces:**
- Consumes: `dados/kovan_painel_contas.csv` (a base **com** lacunas, a mesma que a turma recebe).
- Produces: `painel` (fixture pytest devolvendo `pd.DataFrame`), `visitas_por_segmento(df, tratamento)` devolvendo `pd.Series` indexada por segmento, usada pela Task 4 e pela Task 7.

O CSV já existe, então as asserções passam de primeira. Isso é exatamente o modo de falha descrito na lição 8.2 da metodologia: teste que nunca falhou pode ser teste que não testa nada. Por isso cada teste desta task é escrito primeiro com um valor **deliberadamente errado**, executado para ver a reprovação, e só então corrigido.

- [ ] **Step 1: Escrever o módulo com os valores propositalmente errados**

```python
# -*- coding: utf-8 -*-
"""Trava os números da Aula 02 contra o painel entregue.

O painel lido aqui é `dados/kovan_painel_contas.csv`, a base COM as lacunas,
que é a mesma que a turma recebe. Os valores são transcritos de forma literal:
importar a constante que o material usa faria o teste concordar consigo mesmo.
"""

from pathlib import Path

import pandas as pd
import pytest

RAIZ = Path(__file__).resolve().parents[2]
CSV = RAIZ / "dados" / "kovan_painel_contas.csv"

SEGMENTOS = ("Cauda", "Estrategico", "Medio")


@pytest.fixture(scope="module")
def painel() -> pd.DataFrame:
    return pd.read_csv(CSV)


def visitas_por_segmento(df: pd.DataFrame, tratamento: str) -> pd.Series:
    """Visitas médias por segmento sob cada decisão de tratamento.

    `zero` preenche a ausência com 0; `excluir` descarta a linha sem registro.
    """
    if tratamento == "zero":
        base = df.assign(v=df["visitas_registradas"].fillna(0))
    elif tratamento == "excluir":
        base = df.dropna(subset=["visitas_registradas"]).rename(
            columns={"visitas_registradas": "v"}
        )
    else:
        raise ValueError(f"tratamento desconhecido: {tratamento}")
    return base.groupby("segmento")["v"].mean()


# --- Advertência 1: receita ausente -----------------------------------------


def test_cento_e_sessenta_linhas_sem_receita(painel):
    assert int(painel["receita_brl"].isna().sum()) == 999


def test_toda_receita_ausente_esta_em_conta_ativa(painel):
    faltando = painel[painel["receita_brl"].isna()]
    assert sorted(faltando["status_conta"].unique()) == ["Ativa"]


def test_valor_medio_do_pedido_falta_nas_mesmas_linhas(painel):
    assert painel["receita_brl"].isna().equals(painel["valor_medio_pedido_brl"].isna())


def test_quatorze_lacunas_de_receita_no_segmento_estrategico(painel):
    estrategico = painel[painel["segmento"] == "Estrategico"]
    assert int(estrategico["receita_brl"].isna().sum()) == 14


# --- Advertência 2: engajamento incompleto e enviesado ----------------------


def test_oito_mil_duzentas_e_oitenta_linhas_sem_engajamento(painel):
    assert int(painel["visitas_registradas"].isna().sum()) == 8_280
    assert int(painel["interacoes_crm"].isna().sum()) == 8_280


def test_visita_e_interacao_faltam_sempre_juntas(painel):
    assert painel["visitas_registradas"].isna().equals(painel["interacoes_crm"].isna())


@pytest.mark.parametrize(
    "segmento,esperado",
    [("Estrategico", 0.2942), ("Medio", 0.4990), ("Cauda", 0.5317)],
)
def test_taxa_de_ausencia_de_engajamento_por_segmento(painel, segmento, esperado):
    base = painel[painel["segmento"] == segmento]
    assert base["visitas_registradas"].isna().mean() == pytest.approx(esperado, abs=0.0001)


@pytest.mark.parametrize(
    "segmento,esperado",
    [("Cauda", 2.1012), ("Medio", 2.2959), ("Estrategico", 2.4479)],
)
def test_visitas_medias_tratando_ausente_como_zero(painel, segmento, esperado):
    assert visitas_por_segmento(painel, "zero")[segmento] == pytest.approx(
        esperado, abs=0.0001
    )


@pytest.mark.parametrize(
    "segmento,esperado",
    [("Estrategico", 3.4683), ("Cauda", 4.4864), ("Medio", 4.5827)],
)
def test_visitas_medias_excluindo_ausentes(painel, segmento, esperado):
    assert visitas_por_segmento(painel, "excluir")[segmento] == pytest.approx(
        esperado, abs=0.0001
    )


def test_a_ordem_dos_segmentos_inverte_entre_os_dois_tratamentos(painel):
    """O achado da aula.

    Preenchendo com zero, o segmento estratégico tem a MAIOR média de visitas
    dos três. Excluindo as linhas sem registro, ele tem a MENOR. As duas
    leituras sustentam recomendações opostas sobre cobertura comercial.
    """
    zero = visitas_por_segmento(painel, "zero").sort_values()
    excluir = visitas_por_segmento(painel, "excluir").sort_values()

    assert list(zero.index) == ["Cauda", "Medio", "Estrategico"]
    assert list(excluir.index) == ["Estrategico", "Cauda", "Medio"]

    assert zero.index[-1] == "Estrategico", "com zero, o estrategico e o maior"
    assert excluir.index[0] == "Estrategico", "excluindo, o estrategico e o menor"


@pytest.mark.parametrize(
    "tratamento,rompeu,nao_rompeu",
    [("zero", 1.5063, 2.8291), ("excluir", 2.2476, 3.9280)],
)
def test_visitas_de_quem_rompeu_no_estrategico(painel, tratamento, rompeu, nao_rompeu):
    estrategico = painel[painel["segmento"] == "Estrategico"]
    rompidas = set(
        estrategico.loc[estrategico["status_conta"] == "Encerrada", "conta_id"]
    )
    marcado = estrategico.assign(rompeu=estrategico["conta_id"].isin(rompidas))
    if tratamento == "zero":
        marcado = marcado.assign(v=marcado["visitas_registradas"].fillna(0))
    else:
        marcado = marcado.dropna(subset=["visitas_registradas"]).rename(
            columns={"visitas_registradas": "v"}
        )
    media = marcado.groupby("rompeu")["v"].mean()
    assert media[True] == pytest.approx(rompeu, abs=0.0001)
    assert media[False] == pytest.approx(nao_rompeu, abs=0.0001)


# --- Advertência 3: mudança de taxonomia em 2023Q1 --------------------------


def test_taxonomia_antiga_existe_apenas_em_dois_trimestres(painel):
    antiga = painel[painel["taxonomia_mix"] == "pre_2023"]
    assert sorted(antiga["trimestre"].unique()) == ["2022Q3", "2022Q4"]


@pytest.mark.parametrize(
    "trimestre,esperado",
    [("2022Q4", 1.8484), ("2023Q1", 3.6698), ("2025Q4", 2.5535)],
)
def test_media_de_linhas_de_produto_por_trimestre(painel, trimestre, esperado):
    base = painel[painel["trimestre"] == trimestre]
    assert base["linhas_produto_ativas"].mean() == pytest.approx(esperado, abs=0.0001)


def test_o_salto_da_taxonomia_e_de_noventa_e_oito_e_meio_por_cento(painel):
    media = painel.groupby("trimestre")["linhas_produto_ativas"].mean()
    salto = media["2023Q1"] / media["2022Q4"] - 1
    assert salto == pytest.approx(0.985, abs=0.001)


def test_a_erosao_dentro_da_taxonomia_comparavel_e_de_trinta_por_cento(painel):
    media = painel.groupby("trimestre")["linhas_produto_ativas"].mean()
    queda = media["2025Q4"] / media["2023Q1"] - 1
    assert queda == pytest.approx(-0.304, abs=0.001)


def test_a_janela_completa_inverte_o_sinal_da_erosao(painel):
    """Comparar 2022Q4 com 2025Q4 mostra crescimento onde houve queda."""
    media = painel.groupby("trimestre")["linhas_produto_ativas"].mean()
    aparente = media["2025Q4"] / media["2022Q4"] - 1
    real = media["2025Q4"] / media["2023Q1"] - 1
    assert aparente == pytest.approx(0.381, abs=0.001)
    assert aparente > 0 > real


# --- Advertência 4: devoluções em coluna própria, com sinal negativo --------


def test_devolucoes_nunca_sao_positivas(painel):
    assert int((painel["devolucoes_brl"] > 0).sum()) == 0
    assert int((painel["devolucoes_brl"] < 0).sum()) == 15_999
    assert int((painel["devolucoes_brl"] == 0).sum()) == 619


def test_o_erro_de_sinal_custa_duzentos_e_dezessete_milhoes(painel):
    bruta = painel["receita_brl"].sum()
    devolucoes = painel["devolucoes_brl"].sum()
    assert bruta == pytest.approx(8_406_227_803.71, abs=0.01)
    assert (bruta + devolucoes) == pytest.approx(8_297_527_227.90, abs=0.01)
    assert (bruta - devolucoes) == pytest.approx(8_514_928_379.52, abs=0.01)
    assert abs(2 * devolucoes) == pytest.approx(217_401_151.62, abs=0.01)
```

- [ ] **Step 2: Rodar e confirmar que o teste do valor errado reprova**

Run: `.venv/bin/python -m pytest dados/tests/test_aula02_numeros.py -q`
Expected: FAIL em `test_cento_e_sessenta_linhas_sem_receita`, com `assert 160 == 999`. Todos os outros passam.

Isso é a prova de que a suíte consegue falhar. Se ela passasse inteira aqui, ela não estaria lendo o CSV.

- [ ] **Step 3: Corrigir o valor plantado**

Trocar `== 999` por `== 160` em `test_cento_e_sessenta_linhas_sem_receita`.

- [ ] **Step 4: Rodar a suíte inteira e confirmar verde**

Run: `.venv/bin/python -m pytest dados/tests/test_aula02_numeros.py -q`
Expected: PASS, 27 testes.

- [ ] **Step 5: Provar que o teste da inversão consegue falhar**

Editar temporariamente `visitas_por_segmento` para que `excluir` devolva o mesmo que `zero`:

```python
    if tratamento in ("zero", "excluir"):
        base = df.assign(v=df["visitas_registradas"].fillna(0))
```

Run: `.venv/bin/python -m pytest dados/tests/test_aula02_numeros.py::test_a_ordem_dos_segmentos_inverte_entre_os_dois_tratamentos -q`
Expected: FAIL. **Reverter a edição em seguida** e rodar a suíte de novo, esperando verde.

O teste da inversão é o achado da aula. Um teste que nunca foi visto falhando não prova nada.

- [ ] **Step 6: Rodar a suíte canônica para garantir que nada quebrou**

Run: `.venv/bin/python -m pytest dados/tests -q`
Expected: PASS, incluindo `test_painel_kovan.py`.

- [ ] **Step 7: Commit**

```bash
git add dados/tests/test_aula02_numeros.py
git commit -m "test(dados): trava os numeros da Aula 02 contra o painel entregue

Os quatro defeitos do Exhibit 3 medidos e travados: 160 linhas de receita
ausente todas em conta Ativa, 8.280 sem engajamento com taxa de 29,4% a
53,2% por segmento, o salto de 98,5% na taxonomia de 2023Q1 e as 15.999
devolucoes negativas que custam R\$ 217,4 milhoes num erro de sinal.

O teste da inversao da ordem dos segmentos foi visto falhando contra uma
versao em que os dois tratamentos sao identicos, conforme a licao 8.2 da
metodologia."
```

---

### Task 2: Validador da diretiva editorial

**Files:**
- Create: `tools/check_retorica.py`
- Create: `tools/tests/test_check_retorica.py`

**Interfaces:**
- Produces: `analisar(texto: str) -> list[Achado]`, onde `Achado` é um `NamedTuple` com `(construcao: str, trecho: str, bloqueia: bool)`. `texto_visivel(html: str) -> list[tuple[int, str]]` devolve `(numero_do_slide, texto_sem_marcacao)`. `main(argv: list[str] | None = None) -> int` devolve o código de saída: `0` aprovado, `1` reprovado, `2` nada examinado.

O validador reprova as três construções da diretiva. Reconhecer antítese simétrica por expressão regular gera falso positivo com facilidade, então ela é tratada como **candidata**, reportada para revisão humana, sem reprovar a build. As duas construções mecânicas (paralelismo negativo e escalada com dois-pontos) reprovam.

- [ ] **Step 1: Escrever o teste que falha**

```python
# -*- coding: utf-8 -*-
"""O validador de retórica precisa reprovar o que a diretiva proíbe, e precisa
aprovar a reescrita correspondente. Um validador que só aprova não protege."""

import pytest

from tools.check_retorica import Achado, analisar, texto_visivel


REPROVADOS = [
    ("Alucinação não é defeito, é propriedade do modelo.", "paralelismo negativo"),
    ("Isso é conteúdo, não escolha de estilo.", "paralelismo negativo"),
    ("O prompt é parte da análise, não um acessório.", "paralelismo negativo"),
    ("A quebra não apenas fabrica sinal falso: ela inverte o sinal.", "escalada"),
    ("O painel não só tem lacunas: as lacunas são enviesadas.", "escalada"),
]

APROVADOS = [
    "A ausência de registro de engajamento varia de 29% a 53% entre segmentos.",
    "A quebra de taxonomia produz duas distorções simultâneas.",
    "O tratamento preserva a direção da relação e altera a magnitude em até 49%.",
    "Nenhuma conta comprou linha de produto adicional entre 2022Q4 e 2023Q1.",
    "O erro de sinal em devolucoes_brl custa R$ 217,4 milhões.",
]


@pytest.mark.parametrize("frase,construcao", REPROVADOS)
def test_reprova_as_construcoes_proibidas(frase, construcao):
    achados = analisar(frase)
    assert achados, f"deveria reprovar: {frase}"
    assert any(construcao in a.construcao for a in achados), [a.construcao for a in achados]


@pytest.mark.parametrize("frase", APROVADOS)
def test_aprova_a_reescrita_correspondente(frase):
    assert analisar(frase) == []


def test_antitese_simetrica_e_candidata_e_nao_reprova():
    """Reconhecer antítese por regex gera falso positivo. Ela vira aviso."""
    achados = analisar("O Gemini descobre, o Antigravity registra.")
    assert any("antítese" in a.construcao for a in achados)
    assert all(a.bloqueia is False for a in achados if "antítese" in a.construcao)


def test_texto_visivel_ignora_marcacao_e_numera_os_slides():
    html = (
        '<section class="content-slide"><h2>Título</h2>'
        "<p>Não é decisão, é acidente.</p></section>\n"
        '<section class="quiz-slide"><p>Pergunta neutra.</p></section>'
    )
    trechos = texto_visivel(html)
    assert [n for n, _ in trechos] == [1, 2]
    assert "Não é decisão, é acidente." in trechos[0][1]
    assert "<h2>" not in trechos[0][1]


def test_nada_examinado_nunca_le_como_sucesso():
    """Lição 8.1: zero itens examinados é erro, não aprovação."""
    assert texto_visivel("<html><body></body></html>") == []
```

- [ ] **Step 2: Rodar e confirmar a falha**

Run: `.venv/bin/python -m pytest tools/tests/test_check_retorica.py -q`
Expected: FAIL com `ModuleNotFoundError: No module named 'tools.check_retorica'`.

- [ ] **Step 3: Implementar o validador**

```python
# -*- coding: utf-8 -*-
"""Trava as três construções retóricas proibidas pela diretiva editorial de
15/08/2026.

A referência de escrita do acervo é a linguagem das apresentações da McKinsey:
o título de um slide de conteúdo é a conclusão completa, com o número dentro, e
o corpo sustenta essa conclusão.

Duas construções reprovam a build, porque são mecânicas e reconhecíveis sem
ambiguidade. A terceira (antítese simétrica) é reportada como candidata: fazer
regex decidir sobre ela produz falso positivo em frase legítima, e um validador
que reprova texto correto acaba desligado.

Uso: python3 tools/check_retorica.py [arquivo ...]
Sem argumento, examina os decks, os materiais e o esqueleto da skill.
"""

from __future__ import annotations

import re
import sys
from pathlib import Path
from typing import NamedTuple

RAIZ = Path(__file__).resolve().parents[1]


class Achado(NamedTuple):
    construcao: str
    trecho: str
    bloqueia: bool


# `não é X, é Y` e `é X, não Y`, com X e Y curtos o bastante para serem o
# mesmo sintagma contrastado, que é o que caracteriza a construção.
_NEGATIVO = [
    re.compile(r"n[ãa]o\s+(?:é|s[ãa]o|era|foi)\s+[^,.;:!?]{2,45},\s*(?:e\s+sim|mas\s+sim|é|s[ãa]o)\b", re.I),
    re.compile(r"\b(?:é|s[ãa]o)\s+[^,.;:!?]{2,45},\s*n[ãa]o\s+[^,.;:!?]{2,45}[.;!?]", re.I),
]

_ESCALADA = re.compile(r"n[ãa]o\s+(?:apenas|s[óo]|somente)\s+[^:.;!?]{2,80}:", re.I)

# Duas orações espelhadas: sujeitos diferentes, verbos no mesmo tempo,
# separadas por vírgula ou ponto e vírgula, sem conjunção subordinativa.
_ANTITESE = re.compile(
    r"\b([OA]s?\s+\w+)\s+(\w+[aeiou])\s*[,;]\s*([oa]s?\s+\w+)\s+(\w+[aeiou])\s*[.;]",
    re.I,
)

_TAG = re.compile(r"<[^>]+>")
_SECTION = re.compile(r"<section\b", re.I)


def analisar(texto: str) -> list[Achado]:
    achados: list[Achado] = []
    for rx in _NEGATIVO:
        for m in rx.finditer(texto):
            achados.append(Achado("paralelismo negativo", m.group(0).strip(), True))
    for m in _ESCALADA.finditer(texto):
        achados.append(Achado("escalada com dois-pontos", m.group(0).strip(), True))
    for m in _ANTITESE.finditer(texto):
        achados.append(Achado("antítese simétrica (candidata)", m.group(0).strip(), False))
    return achados


def texto_visivel(html: str) -> list[tuple[int, str]]:
    """Devolve (numero_do_slide, texto sem marcacao) para cada <section>."""
    partes = _SECTION.split(html)[1:]
    saida = []
    for i, parte in enumerate(partes, 1):
        limpo = _TAG.sub(" ", "<section" + parte)
        limpo = re.sub(r"&middot;", " ", limpo)
        limpo = re.sub(r"\s+", " ", limpo).strip()
        saida.append((i, limpo))
    return saida


ALVOS_PADRAO = [
    "aulas/aula01.html",
    "aulas/aula02.html",
    "materiais/aula01-material-de-apoio.html",
    "materiais/aula02-material-de-apoio.html",
    "materiais/skill-limpeza-kovan.md",
    "materiais/caderno-de-hipoteses.md",
]


def main(argv: list[str] | None = None) -> int:
    argv = argv if argv is not None else sys.argv[1:]
    caminhos = [Path(a) for a in argv] if argv else [RAIZ / a for a in ALVOS_PADRAO]

    examinados = 0
    bloqueios = 0
    avisos = 0

    for caminho in caminhos:
        if not caminho.exists():
            continue
        conteudo = caminho.read_text(encoding="utf-8")
        if caminho.suffix == ".html":
            trechos = texto_visivel(conteudo)
        else:
            trechos = [(i, l) for i, l in enumerate(conteudo.splitlines(), 1) if l.strip()]
        for numero, texto in trechos:
            examinados += 1
            for achado in analisar(texto):
                marca = "REPROVA" if achado.bloqueia else "revisar"
                alvo = "slide" if caminho.suffix == ".html" else "linha"
                print(f"{marca}  {caminho.name} {alvo} {numero}: {achado.construcao}")
                print(f"         {achado.trecho}")
                if achado.bloqueia:
                    bloqueios += 1
                else:
                    avisos += 1

    # Licao 8.1: nada medido nunca pode ler como sucesso.
    if examinados == 0:
        print("Nenhum trecho examinado. Verifique os caminhos.")
        return 2

    print(f"\n{examinados} trechos examinados, {bloqueios} reprovações, {avisos} a revisar.")
    return 1 if bloqueios else 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `.venv/bin/python -m pytest tools/tests/test_check_retorica.py -q`
Expected: PASS, 13 testes (5 reprovados, 5 aprovados, e mais 3).

Se algum `APROVADOS` reprovar, o regex está largo demais e precisa ser estreitado. Um validador que reprova texto correto acaba desligado, e aí não protege nada.

- [ ] **Step 5: Rodar contra o acervo existente e registrar o que aparecer**

Run: `.venv/bin/python tools/check_retorica.py aulas/aula01.html materiais/aula01-material-de-apoio.html`

A Aula 01 foi escrita antes da diretiva. **Não corrigir agora.** Copiar a saída para o corpo da mensagem de commit, para que a dívida fique registrada e visível.

- [ ] **Step 6: Commit**

```bash
git add tools/check_retorica.py tools/tests/test_check_retorica.py
git commit -m "tools: validador da diretiva editorial contra paralelismo

Reprova paralelismo negativo e escalada com dois-pontos, que sao mecanicos.
Antitese simetrica vira candidata reportada, porque decidir sobre ela por
regex reprova frase legitima, e validador que reprova texto correto acaba
desligado.

A Aula 01 foi escrita antes da diretiva e nao esta sendo corrigida agora. O
que o validador aponta la fica registrado nesta mensagem."
```

---

### Task 3: Extrair os construtores de slide para `tools/deck_kit.py`

**Files:**
- Create: `tools/deck_kit.py`
- Modify: `tools/montar_deck_aula01.py:28-260` (remover as funções, importar do kit)
- Create: `tools/tests/test_deck_kit.py`

**Interfaces:**
- Produces: `configurar(rodape_texto: str) -> None`, `reiniciar_paginacao() -> None`, `montar(slides: list[str], esqueleto: str, saida: Path) -> int`, mais os construtores `conteudo(titulo, corpo, sobrelinha=None, contexto=None, conclusao=None, rotulo_conclusao="Implicação", conclusao_clara=False, fonte=None, por_passos=False)`, `figura(titulo, arquivo, alt, sobrelinha=None, contexto=None, conclusao=None, rotulo_conclusao="Implicação", fonte=None)`, `figura_embutida(titulo, arquivo, sobrelinha=None, contexto=None, conclusao=None, rotulo_conclusao="Implicação", fonte=None)`, `pratica(numero, tarefa, minutos, formato, entrega, checkpoint, passos, criterio, ambiente="Colab")`, `quiz(sobrelinha, titulo, pergunta, opcoes, cenario)` e `secao(numero, titulo, apoio, condicoes=None)`. Assinaturas idênticas às atuais, com três adições: o parâmetro `ambiente` em `pratica`, o par `configurar`/`reiniciar_paginacao` para o estado global de paginação, e `montar` no lugar do `main()` que hoje escreve o arquivo.

**Por que a extração vem antes do deck da Aula 02:** os construtores são 220 linhas dentro do gerador da Aula 01. Duplicá-las faz qualquer correção de layout precisar de dois lugares, e as convenções travadas por teste passam a valer para uma aula só.

**A armadilha específica:** `pratica()` hoje escreve `f"Prática {numero} · {minutos} minutos · Colab"` com o ambiente fixo no código. A Aula 02 roda em Gemini. O parâmetro `ambiente` entra com valor padrão `"Colab"` justamente para que a saída da Aula 01 não mude.

- [ ] **Step 1: Escrever o teste da extração**

```python
# -*- coding: utf-8 -*-
"""A extração do deck_kit não pode mudar uma vírgula do deck da Aula 01."""

import hashlib
import subprocess
import sys
from pathlib import Path

RAIZ = Path(__file__).resolve().parents[2]
DECK = RAIZ / "aulas" / "aula01.html"


def _sha(caminho: Path) -> str:
    return hashlib.sha256(caminho.read_bytes()).hexdigest()


def test_regerar_a_aula01_produz_o_mesmo_arquivo():
    antes = _sha(DECK)
    subprocess.run(
        [sys.executable, str(RAIZ / "tools" / "montar_deck_aula01.py")],
        cwd=RAIZ, check=True, capture_output=True,
    )
    assert _sha(DECK) == antes, "a extracao mudou a saida do deck da Aula 01"


def test_pratica_usa_colab_por_padrao_e_aceita_outro_ambiente():
    from tools import deck_kit

    deck_kit.reiniciar_paginacao()
    passos = [{"acao": "Subir o painel"}]
    padrao = deck_kit.pratica(1, "Tarefa", 20, "Trios", "Base", "Numero", passos, "Criterio")
    assert "&middot; Colab" in padrao

    deck_kit.reiniciar_paginacao()
    gemini = deck_kit.pratica(
        1, "Tarefa", 20, "Trios", "Base", "Numero", passos, "Criterio", ambiente="Gemini"
    )
    assert "&middot; Gemini" in gemini
    assert "Colab" not in gemini


def test_a_paginacao_reinicia_entre_decks():
    from tools import deck_kit

    deck_kit.reiniciar_paginacao()
    primeiro = deck_kit.secao(1, "T", "A")
    deck_kit.reiniciar_paginacao()
    segundo = deck_kit.secao(1, "T", "A")
    assert primeiro == segundo
```

- [ ] **Step 2: Rodar e confirmar a falha**

Run: `.venv/bin/python -m pytest tools/tests/test_deck_kit.py -q`
Expected: FAIL com `ModuleNotFoundError: No module named 'tools.deck_kit'`.

- [ ] **Step 3: Criar `tools/deck_kit.py`**

Mover, **sem alterar uma linha do corpo**, de `tools/montar_deck_aula01.py`: `_proxima`, `_rodape`, `_cabecalho`, `_conclusao`, `_fonte`, `_fecho`, `conteudo`, `figura`, `figura_embutida`, `pratica`, `quiz`, `secao`, mais as constantes `RAIZ`, `LOGO` e o global `_pagina`.

Quatro mudanças, e apenas estas quatro:

```python
RODAPE_TEXTO = "Módulo 2 &middot; Aula 01"   # sobrescrito por configurar()


def configurar(rodape_texto: str) -> None:
    """Define o texto do rodapé do deck em construção."""
    global RODAPE_TEXTO
    RODAPE_TEXTO = rodape_texto


def reiniciar_paginacao() -> None:
    """Zera o contador de páginas. Obrigatório entre dois decks no mesmo processo."""
    global _pagina
    _pagina = 1


def pratica(numero, tarefa, minutos, formato, entrega, checkpoint, passos, criterio,
            ambiente="Colab") -> str:
    ...
        + _cabecalho(f"Prática {numero} &middot; {minutos} minutos &middot; {ambiente}", tarefa, None)
    ...


def montar(slides: list[str], esqueleto: str, saida: Path) -> int:
    """Escreve o deck e devolve o número da última página."""
    saida.write_text(esqueleto.format(slides="\n".join(slides)), encoding="utf-8")
    return _pagina
```

Criar também `tools/__init__.py` vazio, se ainda não existir, para que `from tools import deck_kit` funcione.

- [ ] **Step 4: Reescrever `tools/montar_deck_aula01.py` para consumir o kit**

Trocar o bloco de definições pelo import, mantendo a docstring do arquivo:

```python
from pathlib import Path

from tools import deck_kit
from tools.deck_kit import conteudo, figura, figura_embutida, pratica, quiz, secao

RAIZ = Path(__file__).resolve().parents[1]
SAIDA = RAIZ / "aulas" / "aula01.html"

deck_kit.configurar("Módulo 2 &middot; Aula 01")
deck_kit.reiniciar_paginacao()
```

e o `main()`:

```python
def main() -> None:
    ultima = deck_kit.montar(SLIDES, ESQUELETO, SAIDA)
    print(f"{SAIDA.name}: {len(SLIDES)} slides, numerados de 2 a {ultima}")
```

- [ ] **Step 5: Rodar o teste da extração**

Run: `.venv/bin/python -m pytest tools/tests/test_deck_kit.py -q`
Expected: PASS, 3 testes. Em especial `test_regerar_a_aula01_produz_o_mesmo_arquivo`.

Se o hash divergir, comparar com `git diff aulas/aula01.html` e corrigir o kit. **Não aceitar a nova saída como boa.**

- [ ] **Step 6: Rodar a suíte de deck existente e os validadores**

Run:
```bash
.venv/bin/python -m pytest tools/tests -q
.venv/bin/python tools/check_brand.py
.venv/bin/python tools/check_slides.py
```
Expected: tudo verde, e `git status` limpo em `aulas/aula01.html`.

- [ ] **Step 7: Commit**

```bash
git add tools/deck_kit.py tools/montar_deck_aula01.py tools/tests/test_deck_kit.py tools/__init__.py
git commit -m "refactor(tools): construtores de slide viram deck_kit compartilhado

A Aula 02 precisa dos mesmos construtores. Duplicar as 220 linhas faria
correcao de layout precisar de dois lugares e deixaria as convencoes
travadas por teste valendo para uma aula so.

pratica() ganha o parametro ambiente, com padrao Colab, porque a Aula 02
roda em Gemini e a sobrelinha trazia o ambiente fixo no codigo.

A extracao esta provada por hash: aula01.html sai byte a byte identico."
```

---

### Task 4: As três figuras da Aula 02

**Files:**
- Create: `tools/gerar_figuras_aula02.py`
- Create: `tools/tests/test_figuras_aula02.py`
- Produce: `assets/img/aula02-inversao-segmentos.png`, `assets/img/aula02-quebra-taxonomia.png`, `assets/img/aula02-mapa-ausencia.png`

**Interfaces:**
- Consumes: `dados/kovan_painel_contas.csv`, e a função `visitas_por_segmento` replicada localmente (o gerador não importa do módulo de teste).
- Produces: três PNG de 1168px de largura.

Ler `tools/gerar_figuras_aula01.py` antes de escrever: paleta, fontes e a convenção de 1168px vêm de lá. **Figura é governada pela largura**, nunca por `max-height`.

- [ ] **Step 1: Escrever o teste**

```python
# -*- coding: utf-8 -*-
"""As figuras da Aula 02 existem, têm a largura da convenção, e o deck usa
exatamente as que o gerador produz."""

import re
from pathlib import Path

import pytest
from PIL import Image

RAIZ = Path(__file__).resolve().parents[2]
IMG = RAIZ / "assets" / "img"

FIGURAS = [
    "aula02-inversao-segmentos.png",
    "aula02-quebra-taxonomia.png",
    "aula02-mapa-ausencia.png",
]


@pytest.mark.parametrize("nome", FIGURAS)
def test_a_figura_existe_e_tem_1168px_de_largura(nome):
    caminho = IMG / nome
    assert caminho.exists(), f"rode tools/gerar_figuras_aula02.py: falta {nome}"
    assert Image.open(caminho).width == 1168


def test_o_deck_referencia_apenas_figuras_que_o_gerador_produz():
    html = (RAIZ / "aulas" / "aula02.html").read_text(encoding="utf-8")
    citadas = set(re.findall(r'src="\.\./assets/img/(aula02-[^"]+)"', html))
    assert citadas, "nenhuma figura da aula02 referenciada no deck"
    assert citadas <= set(FIGURAS), citadas - set(FIGURAS)
```

- [ ] **Step 2: Rodar e confirmar a falha**

Run: `.venv/bin/python -m pytest tools/tests/test_figuras_aula02.py -q`
Expected: FAIL, `falta aula02-inversao-segmentos.png`. O último teste também falha, porque `aulas/aula02.html` ainda não existe.

- [ ] **Step 3: Escrever o gerador**

Três figuras, cada uma respondendo a um slide:

1. **`aula02-inversao-segmentos.png`.** Dois painéis lado a lado, barras horizontais de visitas médias por segmento. À esquerda o tratamento "ausente vira zero" (Cauda 2,10, Médio 2,30, Estratégico 2,45), à direita "excluir sem registro" (Estratégico 3,47, Cauda 4,49, Médio 4,58). Em cada painel as barras ficam **ordenadas do menor para o maior**, e a barra do Estratégico recebe cor de destaque, para que a mudança de posição seja o que salta aos olhos. Mesma escala nos dois eixos.
2. **`aula02-quebra-taxonomia.png`.** Linha de `linhas_produto_ativas` média por trimestre, com faixa vertical marcando 2023Q1, o rótulo do salto (1,85 para 3,67) e a inclinação de queda de 2023Q1 a 2025Q4 destacada.
3. **`aula02-mapa-ausencia.png`.** Matriz de taxa de ausência: colunas do painel nas linhas, segmentos nas colunas, célula com a taxa. Só as colunas com ausência entram (`receita_brl`, `valor_medio_pedido_brl`, `visitas_registradas`, `interacoes_crm`), mais uma linha de referência com uma coluna sem ausência, para que a leitura tenha um zero de comparação.

O esqueleto do arquivo segue `tools/gerar_figuras_aula01.py`: mesma importação de paleta, mesma função de salvamento com `dpi` calculado para dar 1168px de largura, e um `main()` que gera as três.

- [ ] **Step 4: Gerar e conferir visualmente**

Run: `.venv/bin/python tools/gerar_figuras_aula02.py`

Abrir as três e conferir: nenhum rótulo cortado, nenhum texto abaixo de 16px na escala final, e a inversão da figura 1 legível a três metros de distância.

- [ ] **Step 5: Rodar os testes de figura**

Run: `.venv/bin/python -m pytest tools/tests/test_figuras_aula02.py -q -k "largura"`
Expected: PASS nos três. O teste do deck continua falhando até a Task 5.

- [ ] **Step 6: Commit**

```bash
git add tools/gerar_figuras_aula02.py tools/tests/test_figuras_aula02.py assets/img/aula02-*.png
git commit -m "feat(aula02): as tres figuras do dia, desenhadas em 1168px

A da inversao ordena as barras dentro de cada painel e destaca o
estrategico, para que a mudanca de posicao entre os dois tratamentos seja o
que a sala enxerga primeiro."
```

---

### Task 5: O deck

**Files:**
- Create: `tools/montar_deck_aula02.py`
- Create: `tools/tests/test_deck_aula02.py`
- Produce: `aulas/aula02.html`

**Interfaces:**
- Consumes: `tools.deck_kit` (Task 3), as figuras da Task 4, os números da Task 1.
- Produces: `aulas/aula02.html`, entre 28 e 34 slides.

**Roteiro do deck**, seguindo a agenda em minutos da seção 4 da spec. Cada prática vira um slide `pratica(..., ambiente="Gemini")`.

| # | Tipo | Conteúdo |
|---|---|---|
| 1 | abertura | Aula 02, "O dado sujo é o case", 15/08/2026 |
| 2 | conteúdo | Resgate: o que a Aula 01 deixou pronto, e as contagens divergentes |
| 3 | conteúdo | O contrato do dia e o entregável |
| 4 | divisor | Bloco 1: qualidade de dado |
| 5 | conteúdo | Onde a limpeza mora dentro da EDA |
| 6 | conteúdo | As seis dimensões de qualidade de dado |
| 7 | prática | Prática 1, perfilamento cego, 20 minutos, Gemini |
| 8 | divisor | Bloco 2: as quatro advertências |
| 9 | conteúdo | Advertência 1, receita ausente: 160 linhas, todas em conta Ativa |
| 10 | figura | `aula02-mapa-ausencia.png` |
| 11 | conteúdo | Advertência 2, engajamento: 8.280 linhas, taxa de 29,4% a 53,2% |
| 12 | figura | `aula02-quebra-taxonomia.png`, advertência 3 |
| 13 | conteúdo | Advertência 4, devoluções: R$ 217,4 milhões no erro de sinal |
| 14 | quiz | Verificação 2 da spec, sobre a média de linhas de produto |
| 15 | prática | Prática 2, mecanismo de ausência, 20 minutos, Gemini |
| 16 | conteúdo | MCAR, MAR e MNAR, nomeando o que a Prática 2 mediu |
| 17 | divisor | Bloco 3: a decisão de tratamento |
| 18 | conteúdo | Excluir, imputar, sinalizar, e o que cada uma assume |
| 19 | conteúdo | Como se mede o custo de uma decisão de tratamento |
| 20 | prática | Prática 3, o confronto, 30 minutos, Gemini |
| 21 | figura | `aula02-inversao-segmentos.png`, revelada depois do confronto |
| 22 | quiz | Verificação 1 da spec, sobre as duas ordens |
| 23 | divisor | Bloco 4: registrar a decisão |
| 24 | conteúdo | O critério que separa Gemini e Antigravity (a tabela da seção 2 da spec) |
| 25 | conteúdo | Anatomia da skill de limpeza em Markdown |
| 26 | prática | Prática 4, preencher e executar a skill, 30 minutos |
| 27 | conteúdo | Amarração: o que fica pronto e o que a Aula 03 pega |
| 28 | conteúdo | Referências e nota metodológica |
| 29 | encerramento | Próximo encontro, 22/08 |

**Restrição de título, por tipo de slide.** Conteúdo leva título afirmativo com o número dentro. Exemplos aceitos: "A ausência de registro de engajamento varia de 29% a 53% entre segmentos"; "As devoluções já chegam negativas, e o erro de sinal custa R$ 217,4 milhões". Prática, quiz e divisor **não** levam título afirmativo.

- [ ] **Step 1: Escrever o teste do deck**

```python
# -*- coding: utf-8 -*-
"""Convenções do deck da Aula 02.

Reaproveita as regras de tools/tests/test_deck_aula01.py e acrescenta as da
diretiva editorial de 15/08.
"""

import re
from pathlib import Path

import pytest

from tools.check_retorica import analisar, texto_visivel

RAIZ = Path(__file__).resolve().parents[2]
DECK = RAIZ / "aulas" / "aula02.html"


@pytest.fixture(scope="module")
def html() -> str:
    assert DECK.exists(), "rode tools/montar_deck_aula02.py"
    return DECK.read_text(encoding="utf-8")


def test_secoes_abrem_e_fecham(html):
    assert html.count("<section") == html.count("</section>")


def test_rodape_numerado_em_sequencia(html):
    paginas = [int(n) for n in re.findall(r'class="footer-page">(\d+)<', html)]
    assert paginas == list(range(2, 2 + len(paginas)))


def test_todo_src_local_existe(html):
    for src in re.findall(r'src="(\.\./[^"]+)"', html):
        alvo = (RAIZ / "aulas" / src).resolve()
        assert alvo.exists(), src


def test_toda_imagem_tem_texto_alternativo(html):
    for tag in re.findall(r"<img\b[^>]*>", html):
        assert 'alt="' in tag, tag


def test_convencoes_editoriais(html):
    assert "—" not in html, "em dash proibido"
    assert not re.search(r"[\U0001F300-\U0001FAFF☀-➿]", html), "emoji proibido"


def test_o_deck_nao_usa_paralelismo(html):
    """A diretiva editorial de 15/08, travada por validador."""
    problemas = []
    for numero, texto in texto_visivel(html):
        for achado in analisar(texto):
            if achado.bloqueia:
                problemas.append((numero, achado.construcao, achado.trecho))
    assert not problemas, problemas


def test_titulo_de_conteudo_e_afirmativo(html):
    for bloco in re.findall(r'<section class="content-slide".*?</section>', html, re.S):
        titulo = re.search(r"<h2>(.*?)</h2>", bloco, re.S)
        if not titulo:
            continue
        texto = re.sub(r"<[^>]+>", "", titulo.group(1)).strip()
        assert not texto.endswith("?"), f"pergunta em slide de conteudo: {texto}"


def test_titulo_de_quiz_nao_entrega_o_gabarito(html):
    for bloco in re.findall(r'<section class="quiz-slide".*?</section>', html, re.S):
        titulo = re.search(r"<h2>(.*?)</h2>", bloco, re.S)
        texto = re.sub(r"<[^>]+>", "", titulo.group(1)).strip()
        assert len(texto.split()) <= 8, f"titulo de quiz longo demais: {texto}"


def test_todo_slide_de_conteudo_fecha_com_implicacao(html):
    blocos = re.findall(r'<section class="content-slide".*?</section>', html, re.S)
    assert blocos
    for bloco in blocos:
        assert 'class="fecho"' in bloco, bloco[:200]


def test_quiz_tem_uma_resposta_correta_por_pergunta(html):
    for bloco in re.findall(r'<section class="quiz-slide".*?</section>', html, re.S):
        assert bloco.count('data-correct="true"') == 1


def test_a_alternativa_correta_nao_e_a_mais_longa(html):
    for bloco in re.findall(r'<section class="quiz-slide".*?</section>', html, re.S):
        itens = re.findall(r'data-correct="(true|false)"[^>]*>([^<]+)</li>', bloco)
        certa = [t for c, t in itens if c == "true"][0]
        assert len(certa) <= max(len(t) for _, t in itens), certa


def test_as_praticas_rodam_em_gemini(html):
    praticas = re.findall(r'<section class="exercise-slide".*?</section>', html, re.S)
    assert len(praticas) == 4
    for bloco in praticas:
        assert "Gemini" in bloco
        assert "Colab" not in bloco


def test_o_deck_cita_os_numeros_travados(html):
    """Nenhum número do case sem teste. Estes vêm de test_aula02_numeros.py."""
    for numero in ("160", "8.280", "29,4", "53,2", "98,5", "217,4"):
        assert numero in html, numero
```

- [ ] **Step 2: Rodar e confirmar a falha**

Run: `.venv/bin/python -m pytest tools/tests/test_deck_aula02.py -q`
Expected: FAIL no fixture, "rode tools/montar_deck_aula02.py".

- [ ] **Step 3: Escrever o gerador do deck**

Cabeçalho do arquivo:

```python
# -*- coding: utf-8 -*-
"""Monta aulas/aula02.html.

Gerado, nunca editado a mao: a numeracao de rodape e o fechamento de secao sao
garantidos aqui, e ja se perderam no HTML duas vezes.

Diretiva editorial de 15/08/2026: sem paralelismo negativo, sem antitese
simetrica, sem escalada com dois-pontos. Titulo de slide de conteudo e a
conclusao completa, com o numero dentro. Travado por tools/check_retorica.py.

Uso: python3 tools/montar_deck_aula02.py
"""

from pathlib import Path

from tools import deck_kit
from tools.deck_kit import conteudo, figura, pratica, quiz, secao

RAIZ = Path(__file__).resolve().parents[1]
SAIDA = RAIZ / "aulas" / "aula02.html"

deck_kit.configurar("Módulo 2 &middot; Aula 02")
deck_kit.reiniciar_paginacao()

SLIDES: list[str] = []
```

**Os títulos, já escritos.** É a parte que a diretiva editorial restringe, então ela não fica a critério de quem implementa. Slide de conteúdo leva a conclusão completa com o número dentro; prática, quiz e divisor levam o nome da tarefa ou do objeto.

| # | Tipo | Título exato |
|---|---|---|
| 2 | conteúdo | Três mesas aplicaram o mesmo critério de ruptura e chegaram a contagens diferentes de 34 |
| 3 | conteúdo | O entregável do dia exige uma decisão declarada para cada uma das quatro advertências do Exhibit 3 |
| 4 | divisor | Qualidade de dado |
| 5 | conteúdo | As decisões de preparação determinam os números que a análise exploratória produz |
| 6 | conteúdo | Seis dimensões descrevem a qualidade de uma base: completude, validade, consistência, unicidade, acurácia e temporalidade |
| 7 | prática | Perfil de qualidade do painel |
| 8 | divisor | As quatro advertências do Exhibit 3 |
| 9 | conteúdo | 160 linhas do painel não têm receita registrada, e todas pertencem a contas ativas |
| 10 | figura | A ausência se concentra em quatro colunas e varia com o segmento |
| 11 | conteúdo | A ausência de registro de engajamento varia de 29,4% a 53,2% entre segmentos |
| 12 | figura | A média de linhas de produto sobe 98,5% em 2023Q1 sem nenhuma conta comprar item adicional |
| 13 | conteúdo | As devoluções chegam com sinal negativo, e inverter esse sinal desloca R$ 217,4 milhões da receita líquida |
| 14 | quiz | A média de linhas de produto |
| 15 | prática | O mecanismo da ausência |
| 16 | conteúdo | O engajamento ausente segue o mecanismo MAR: o segmento observado explica a probabilidade de a linha faltar |
| 17 | divisor | A decisão de tratamento |
| 18 | conteúdo | Excluir, imputar e sinalizar assumem coisas diferentes sobre o que a lacuna representa |
| 19 | conteúdo | O custo de uma decisão de tratamento se mede recalculando o indicador sob cada alternativa |
| 20 | prática | O custo da decisão de tratamento |
| 21 | figura | O segmento estratégico passa da maior para a menor média de visitas conforme o tratamento da lacuna |
| 22 | quiz | As duas ordens |
| 23 | divisor | Registrar a decisão |
| 24 | conteúdo | Cinco critérios separam os dois ambientes de trabalho com IA, e a reexecução por terceiro é a exigência do entregável |
| 25 | conteúdo | A skill de limpeza registra, para cada advertência, a decisão tomada e o custo medido |
| 26 | prática | A skill de limpeza do grupo |
| 27 | conteúdo | A base tratada de hoje sustenta a análise bivariada da Aula 03 e o Artefato 1 |
| 28 | conteúdo | Referências e nota metodológica |

Nenhum desses títulos usa contraste espelhado, e cada título de conteúdo carrega o número que o corpo do slide sustenta. Ao escrever o corpo, manter a mesma disciplina: `test_o_deck_nao_usa_paralelismo` reprova a build.

Dois exemplos concretos de corpo, um de conteúdo e um de prática, para fixar o padrão:

```python
SLIDES.append(conteudo(
    "A ausência de registro de engajamento varia de 29,4% a 53,2% entre segmentos",
    '        <div class="concept-cards quatro">\n'
    '          <div class="concept-card"><h3>Estratégico</h3><p>29,4% das linhas sem visita nem interação registrada.</p></div>\n'
    '          <div class="concept-card"><h3>Médio</h3><p>49,9%.</p></div>\n'
    '          <div class="concept-card"><h3>Cauda</h3><p>53,2%.</p></div>\n'
    '          <div class="concept-card"><h3>Total</h3><p>8.280 das 16.618 linhas do painel.</p></div>\n'
    "        </div>\n",
    sobrelinha="Advertência 2 de 4",
    conclusao="A taxa de ausência acompanha o segmento, de modo que preencher a lacuna com zero desloca mais as contas de cauda do que as estratégicas.",
    fonte="Painel Kovan, 16.618 registros conta-trimestre. Exhibit 3.",
))

SLIDES.append(pratica(
    3,
    "O custo da decisão de tratamento",
    30,
    "Trios, metade da sala em cada tratamento",
    "Visitas médias por segmento, nos três segmentos",
    "Os dois quadros no projetor, lado a lado",
    [
        {"acao": "Mesas 1 e 2: preencher a ausência de visitas com zero",
         "prompt": "Trate visitas_registradas ausente como 0 e calcule a média por segmento. Mostre o código."},
        {"acao": "Mesas 3 e 4: excluir as linhas sem registro de visita",
         "prompt": "Descarte as linhas em que visitas_registradas está ausente e calcule a média por segmento. Mostre o código."},
        {"acao": "Escrever os três números no quadro da sua metade",
         "detalhe": "Cauda, Médio e Estratégico, com duas casas decimais."},
        {"acao": "Defender a leitura para a outra metade",
         "detalhe": "Qual recomendação sobre cobertura comercial o seu número sustenta?"},
    ],
    "Cada metade consegue dizer qual decisão de tratamento produziu o seu número.",
    ambiente="Gemini",
))
```

O esqueleto HTML é o mesmo de `montar_deck_aula01.py`, com o `<title>` trocado para `Aula 02 &middot; O dado sujo é o case &middot; MBA Inteli x Lenovo` e o comentário de "gerado por" apontando para este arquivo.

- [ ] **Step 4: Gerar o deck**

Run: `.venv/bin/python tools/montar_deck_aula02.py`
Expected: `aula02.html: 29 slides, numerados de 2 a 30`.

- [ ] **Step 5: Rodar os testes do deck**

Run: `.venv/bin/python -m pytest tools/tests/test_deck_aula02.py tools/tests/test_figuras_aula02.py -q`
Expected: PASS.

Se `test_o_deck_nao_usa_paralelismo` reprovar, **reescrever o slide**, nunca afrouxar o validador.

- [ ] **Step 6: Rodar os validadores de marca, layout e retórica**

Run:
```bash
.venv/bin/python tools/check_brand.py
.venv/bin/python tools/check_slides.py
.venv/bin/python tools/check_retorica.py aulas/aula02.html
.venv/bin/python tools/medir_ocupacao.py
```
Expected: sem estouro de 1280x720, sem sobreposição, zero reprovações de retórica.

- [ ] **Step 7: Verificação manual, que nenhum validador cobre**

Subir `python3 -m http.server 8931`, abrir `http://localhost:8931/aulas/aula02.html`, e:
- responder os dois quizzes e conferir o feedback de cada alternativa;
- percorrer os slides de figura conferindo que a figura ocupa a largura do quadro;
- conferir que os quatro slides de prática dizem Gemini na sobrelinha.

`check_slides.py` nunca clica em nada.

- [ ] **Step 8: Commit**

```bash
git add tools/montar_deck_aula02.py tools/tests/test_deck_aula02.py aulas/aula02.html
git commit -m "feat(aula02): deck de preparacao e limpeza, 29 slides

Os quatro defeitos do Exhibit 3 com os numeros travados por
dados/tests/test_aula02_numeros.py, quatro praticas em Gemini e a inversao
da ordem dos segmentos revelada depois do confronto da Pratica 3.

Escrito sob a diretiva editorial de 15/08: sem paralelismo, titulo de
conteudo e a conclusao completa com o numero dentro. Travado por
tools/check_retorica.py."
```

---

### Task 6: O esqueleto da skill de limpeza

**Files:**
- Create: `materiais/skill-limpeza-kovan.md`
- Create: `tools/tests/test_skill_limpeza.py`

**Interfaces:**
- Consumes: nada.
- Produces: o arquivo que a Prática 4 preenche e a Task 8 documenta.

O esqueleto tem uma linha obrigatória para **cada uma das quatro advertências**, porque omissão conta como decisão de manter o dado como veio. É Markdown puro, sem sintaxe proprietária, para funcionar colado no Gemini e lido pelo agente do Antigravity.

- [ ] **Step 1: Escrever o teste**

```python
# -*- coding: utf-8 -*-
"""O esqueleto da skill precisa cobrir as quatro advertências e permanecer
portátil entre agentes."""

from pathlib import Path

import pytest

from tools.check_retorica import analisar

RAIZ = Path(__file__).resolve().parents[2]
SKILL = RAIZ / "materiais" / "skill-limpeza-kovan.md"


@pytest.fixture(scope="module")
def texto() -> str:
    return SKILL.read_text(encoding="utf-8")


@pytest.mark.parametrize(
    "coluna",
    ["receita_brl", "visitas_registradas", "interacoes_crm", "taxonomia_mix", "devolucoes_brl"],
)
def test_o_esqueleto_pede_decisao_para_cada_coluna_afetada(texto, coluna):
    assert coluna in texto


def test_o_esqueleto_tem_uma_lacuna_por_advertencia(texto):
    assert texto.count("PREENCHER") == 4


def test_o_esqueleto_exige_o_custo_medido(texto):
    assert "custo" in texto.lower()
    assert "número antes" in texto.lower() or "numero antes" in texto.lower()


def test_o_esqueleto_e_markdown_puro(texto):
    """Sem sintaxe proprietária de agente: ele roda colado no Gemini e lido
    por um agente que trabalha sobre arquivos."""
    for proibido in ("---\nname:", "<system>", "{{", "@workspace"):
        assert proibido not in texto, proibido


def test_o_esqueleto_respeita_a_diretiva_editorial(texto):
    problemas = [a for linha in texto.splitlines() for a in analisar(linha) if a.bloqueia]
    assert not problemas, problemas


def test_convencoes_editoriais(texto):
    assert "—" not in texto
```

- [ ] **Step 2: Rodar e confirmar a falha**

Run: `.venv/bin/python -m pytest tools/tests/test_skill_limpeza.py -q`
Expected: FAIL com `FileNotFoundError`.

- [ ] **Step 3: Escrever o esqueleto**

```markdown
# Skill de limpeza: painel de contas Kovan

Instruções de tratamento do painel `kovan_painel_contas.csv`. Este arquivo é
lido por um agente antes de qualquer análise: cole-o no início da conversa no
Gemini, ou deixe-o na pasta do projeto para o agente encontrar.

Preenchido pelo grupo: ____________________   Data: ____ / ____ / 2026

## Objetivo

Produzir a base tratada do painel Kovan, com cada decisão de tratamento
registrada e com o custo de cada uma medido em número.

## Regras que valem para toda a execução

1. Não estime, não interpole e não complete nenhum valor que não esteja na
   base. Se a informação não existir, responda que não existe.
2. Mostre o código que produziu cada número antes de apresentar o número.
3. Ao aplicar qualquer tratamento, imprima a contagem de linhas afetadas.

## Passo 1: perfilamento

Para cada coluna, informe tipo, contagem de nulos, mínimo, máximo e número de
valores distintos. Não trate nada ainda.

## Passo 2: receita ausente (`receita_brl`, `valor_medio_pedido_brl`)

Situação na base: linhas de conta ativa sem receita registrada, por atraso de
fechamento contábil.

Decisão do grupo: PREENCHER

Justificativa em uma frase: ______________________________________________

Custo medido (número antes e número depois): ____________________________

## Passo 3: engajamento comercial (`visitas_registradas`, `interacoes_crm`)

Situação na base: linhas sem registro de visita nem de interação. A taxa de
ausência varia por segmento.

Decisão do grupo: PREENCHER

Justificativa em uma frase: ______________________________________________

Custo medido (número antes e número depois): ____________________________

## Passo 4: mudança de taxonomia (`taxonomia_mix`, `linhas_produto_ativas`)

Situação na base: a taxonomia de mix mudou em 2023Q1, e a contagem de linhas de
produto não é comparável através desse corte.

Decisão do grupo: PREENCHER

Justificativa em uma frase: ______________________________________________

Custo medido (número antes e número depois): ____________________________

## Passo 5: devoluções (`devolucoes_brl`)

Situação na base: a coluna é independente da receita e traz o próprio sinal.

Decisão do grupo: PREENCHER

Justificativa em uma frase: ______________________________________________

Custo medido (número antes e número depois): ____________________________

## Passo 6: saída

Grave a base tratada e imprima, em uma tabela, a contagem de linhas antes e
depois de cada um dos passos 2 a 5.

## Critério de aceite

Executar este arquivo contra a base crua reproduz os mesmos números que o grupo
apresentou na Prática 3. Se o número não bate, o arquivo não descreve o que a
mesa fez.
```

- [ ] **Step 4: Rodar o teste**

Run: `.venv/bin/python -m pytest tools/tests/test_skill_limpeza.py -q`
Expected: PASS, 10 testes.

- [ ] **Step 5: Executar o esqueleto de verdade, com gabarito, no Gemini**

Preencher uma cópia local com as decisões de referência (excluir em todos os
passos), colar no Gemini com o CSV, e conferir que os números batem com os da
Task 1. Registrar o resultado nas notas da Task 9. Nenhum validador cobre isso.

- [ ] **Step 6: Commit**

```bash
git add materiais/skill-limpeza-kovan.md tools/tests/test_skill_limpeza.py
git commit -m "feat(aula02): esqueleto da skill de limpeza que o aluno leva embora

Markdown puro, sem sintaxe proprietaria, para funcionar colado no Gemini e
lido pelo agente do Antigravity. Uma lacuna obrigatoria por advertencia do
Exhibit 3, porque omissao conta como decisao de manter o dado como veio."
```

---

### Task 7: O notebook

**Files:**
- Create: `tools/montar_notebook_aula02.py`
- Produce: `notebooks/aula02_limpeza.ipynb`

**Interfaces:**
- Consumes: os valores da Task 1.
- Produces: notebook com badge do Colab na primeira célula e carga de dados que cai para a URL bruta do repositório quando o caminho local não existe.

O notebook é o **plano B declarado** se o upload do CSV no Gemini falhar, e é onde o grupo exporta o entregável. Seguir a estrutura de `tools/montar_notebook_aula01.py`.

- [ ] **Step 1: Ler o gerador da Aula 01**

Run: `.venv/bin/python -c "print(open('tools/montar_notebook_aula01.py').read()[:3000])"`

Reaproveitar a função de montagem de célula e a célula de carga com fallback.

- [ ] **Step 2: Escrever o gerador**

Seções do notebook, na ordem das práticas:

1. Badge do Colab e carga do painel, com fallback para a URL bruta.
2. Prática 1: perfilamento coluna a coluna.
3. Prática 2: taxa de ausência por segmento, com a tabela de 29,4% a 53,2%.
4. Prática 3: as duas funções de tratamento e a comparação lado a lado.
5. Prática 4: célula que lê a `skill-limpeza-kovan.md` preenchida e executa os passos.
6. Exportação do entregável: base tratada em CSV mais a tabela de custo.

- [ ] **Step 3: Gerar e executar de ponta a ponta**

Run:
```bash
.venv/bin/python tools/montar_notebook_aula02.py
.venv/bin/python -m jupyter nbconvert --to notebook --execute notebooks/aula02_limpeza.ipynb --stdout > /dev/null
```
Expected: execução sem erro. Se `jupyter` não estiver em `.venv`, instalar via `requirements-dev.txt` antes.

- [ ] **Step 4: Conferir que os números do notebook batem com a Task 1**

Run: `.venv/bin/python -m pytest dados/tests/test_aula02_numeros.py -q`
Expected: PASS. Comparar a saída da célula da Prática 3 com os valores da tabela de constantes canônicas.

- [ ] **Step 5: Commit**

```bash
git add tools/montar_notebook_aula02.py notebooks/aula02_limpeza.ipynb
git commit -m "feat(aula02): notebook de limpeza, plano B do Gemini e saida do entregavel

Carga com fallback para a URL bruta do repositorio, que e o caminho dentro
do Colab, e celula final que exporta a base tratada com a tabela de custo
por decisao."
```

---

### Task 8: O material de apoio

**Files:**
- Create: `materiais/aula02-material-de-apoio.html`

**Interfaces:**
- Consumes: a estrutura de `materiais/aula01-material-de-apoio.html` (TOC lateral e navegação).

O material aprofunda o que o slide só pôde apontar. **Não é o roteiro do deck em prosa.**

Sete seções:

1. **Mecanismo de ausência aplicado coluna a coluna.** MCAR, MAR e MNAR definidos, e a classificação de cada uma das quatro colunas com lacuna neste painel, com o teste que sustenta a classificação.
2. **Por que preencher com zero desloca a cauda.** A aritmética por trás da inversão da ordem dos segmentos, passo a passo.
3. **A quebra de taxonomia trimestre a trimestre.** A tabela completa das 14 médias, e as três formas de tratar a quebra, com o efeito de cada uma.
4. **Devoluções e o erro de sinal.** Por que a coluna vem negativa, e as duas leituras da receita líquida em reais.
5. **Os prompts completos das quatro práticas**, no formato exato para colar.
6. **A skill de limpeza em outros agentes.** Onde cada ambiente espera encontrar o arquivo de instruções, e o que muda entre eles.
7. **Leitura complementar do professor.**

- [ ] **Step 1: Escrever o material seguindo a estrutura da Aula 01**

Copiar o esqueleto de `materiais/aula01-material-de-apoio.html` (TOC lateral, navegação, CSS da marca) e substituir o conteúdo.

- [ ] **Step 2: Rodar o validador de retórica e o de marca**

Run:
```bash
.venv/bin/python tools/check_retorica.py materiais/aula02-material-de-apoio.html
.venv/bin/python tools/check_brand.py
```
Expected: zero reprovações.

- [ ] **Step 3: Conferir a responsividade com viewport real**

`--window-size` de headless não é viewport CSS. Usar Playwright com `viewport` explícito e comparar `scrollWidth` com `clientWidth` em 768px de largura:

```bash
.venv/bin/python - <<'PY'
from playwright.sync_api import sync_playwright
from pathlib import Path
alvo = Path("materiais/aula02-material-de-apoio.html").resolve().as_uri()
with sync_playwright() as p:
    b = p.chromium.launch()
    pg = b.new_page(viewport={"width": 768, "height": 1024})
    pg.goto(alvo)
    sw = pg.evaluate("document.documentElement.scrollWidth")
    cw = pg.evaluate("document.documentElement.clientWidth")
    print("scrollWidth", sw, "clientWidth", cw, "OK" if sw <= cw else "ROLAGEM HORIZONTAL")
    b.close()
PY
```
Expected: `OK`.

- [ ] **Step 4: Commit**

```bash
git add materiais/aula02-material-de-apoio.html
git commit -m "feat(aula02): material de apoio, sete secoes

Traz o que a projecao nao comporta: o mecanismo de ausencia classificado
coluna a coluna, a aritmetica da inversao da ordem dos segmentos, as 14
medias da taxonomia e os prompts completos das quatro praticas."
```

---

### Task 9: Notas de condução

**Files:**
- Create: `docs/notas-do-professor/aula02.md` (**não commitado**, o diretório está no `.gitignore`)

Seguir a estrutura de `docs/notas-do-professor/aula01.md`.

- [ ] **Step 1: Escrever as notas**

Seções obrigatórias:

- **Antes de começar:** conferir que o upload de CSV no Gemini funciona na conta da sala; deixar o notebook aberto e rodado uma vez; testar o Antigravity na máquina do professor com a pasta do case já aberta; mandar o material de apoio no canal antes da aula.
- **O que a Aula 01 deixou pronto**, e quais contagens de ruptura foram para o quadro.
- **Prática 1, gabarito:** a IA costuma achar a receita ausente e as devoluções negativas. Ela costuma **perder** o viés do engajamento por segmento, porque perfilar coluna a coluna não cruza a ausência com o segmento. É esse buraco que abre a Prática 2.
- **Prática 2, gabarito:** 29,4%, 49,9%, 53,2%. Classificação: MAR, com o segmento como variável observada que explica a ausência.
- **Prática 3, gabarito:** as duas tabelas de visitas por segmento, e a instrução de **não entregar a inversão**: deixar cada metade apresentar antes de projetar a figura 21.
- **Prática 4, gabarito:** o esqueleto preenchido com as decisões de referência, e os números que ele reproduz.
- **Perguntas socráticas de reserva**, com resposta esperada e o erro comum que cada uma revela.
- **Riscos do dia:** upload do CSV bloqueado pela conta corporativa; Antigravity não instala; um grupo terminar a Prática 3 em 10 minutos (extensão: repetir o confronto sobre `interacoes_crm`); a turma querer discutir Caminho A ou B.
- **Amarração de saída.**

- [ ] **Step 2: Confirmar que o arquivo não entra no git**

Run: `git status --short docs/notas-do-professor/`
Expected: saída vazia. Se aparecer, o `.gitignore` está errado e precisa ser corrigido antes de qualquer commit.

---

### Task 10: ADRs, planejamento e publicação

**Files:**
- Create: `docs/adrs/ADR-003-gemini-e-antigravity-nas-praticas.md`
- Create: `docs/adrs/ADR-004-skill-de-agente-como-artefato-do-aluno.md`
- Modify: `PLANEJAMENTO_AULA_A_AULA.md:81-101` (a seção S2)
- Modify: `tools/build_site.py:32-40` (allowlist)
- Modify: `index.html` (cartão da Aula 02)

- [ ] **Step 1: Escrever os dois ADRs**

Seguir a estrutura de `docs/adrs/ADR-002`: Data, Status, Decisores, Contexto, Decisão, Motivações, Riscos conhecidos com mitigação, Consequências, ADRs relacionadas. O conteúdo de cada um está na seção 7 da spec.

O ADR-003 precisa dizer explicitamente que **emenda o ADR-002**, e o ADR-002 ganha uma linha em "ADRs relacionadas" apontando para o 003.

Nada que revele o achado plantado no dataset entra em `docs/adrs/`: esse diretório é público.

- [ ] **Step 2: Atualizar a seção S2 do planejamento**

Substituir a seção atual pela agenda em minutos da seção 4 da spec, mais os objetivos de aprendizagem, as verificações do encontro, o entregável e a lista de artefatos. Acrescentar uma subseção **"Divergência registrada"** com o texto da seção 5 da spec, incluindo os números medidos (168 contra 169) que motivaram a troca da coluna do confronto.

- [ ] **Step 3: Acrescentar os arquivos novos à allowlist**

```python
ARQUIVOS = [
    ...
    ("aulas/aula02.html", "aulas/aula02.html"),
    ("materiais/aula02-material-de-apoio.html", "materiais/aula02-material-de-apoio.html"),
    ("materiais/skill-limpeza-kovan.md", "materiais/skill-limpeza-kovan.md"),
    ("notebooks/aula02_limpeza.ipynb", "notebooks/aula02_limpeza.ipynb"),
    ("assets/img/aula02-inversao-segmentos.png", "assets/img/aula02-inversao-segmentos.png"),
    ("assets/img/aula02-quebra-taxonomia.png", "assets/img/aula02-quebra-taxonomia.png"),
    ("assets/img/aula02-mapa-ausencia.png", "assets/img/aula02-mapa-ausencia.png"),
]
```

- [ ] **Step 4: Acrescentar o cartão da Aula 02 ao portal**

Seguir o cartão da Aula 01 em `index.html`. **O link do notebook aponta para o arquivo no GitHub, nunca por caminho relativo:** servido por caminho relativo, o `.ipynb` chega ao navegador como JSON cru.

- [ ] **Step 5: Rodar a suíte inteira e os validadores**

Run:
```bash
.venv/bin/python -m pytest dados/tests tools/tests -q
.venv/bin/python tools/check_brand.py
.venv/bin/python tools/check_slides.py
.venv/bin/python tools/check_retorica.py
.venv/bin/python tools/build_site.py
```
Expected: tudo verde, e `build_site.py` sem nenhum arquivo faltando na allowlist.

- [ ] **Step 6: Conferir que nada sigiloso entrou**

Run: `git status --short && git diff --cached --name-only`
Expected: nenhum caminho sob `recebidos/` nem sob `docs/notas-do-professor/`.

- [ ] **Step 7: Commit**

```bash
git add docs/adrs/ADR-003-*.md docs/adrs/ADR-004-*.md docs/adrs/ADR-002-*.md \
        PLANEJAMENTO_AULA_A_AULA.md tools/build_site.py index.html
git commit -m "docs(aula02): ADRs de ambiente e de artefato, planejamento e portal

ADR-003 emenda o ADR-002: as praticas passam a rodar em Gemini, com o
Antigravity como demonstracao projetada e o Colab como plano B declarado.
ADR-004 registra a skill de agente em Markdown como o artefato que o aluno
leva para o trabalho.

O planejamento da S2 ganha a agenda em minutos e registra a divergencia
medida: o PBL prescrito rendia 168 contra 169 rupturas, e a coluna do
confronto passou a ser visitas_registradas."
```

---

## Ordem de execução e dependências

```
Task 1 (números)  ──┬─→ Task 4 (figuras) ──┐
                    ├─→ Task 6 (skill)     │
                    └─→ Task 7 (notebook)  │
Task 2 (retórica) ──┬────────────────────→ Task 5 (deck) ─→ Task 10
Task 3 (deck_kit) ──┘                      │
                                Task 8 (material) ─┘
                                Task 9 (notas, a qualquer momento)
```

Tasks 1, 2 e 3 podem correr em qualquer ordem entre si. A Task 5 depende das três anteriores mais a Task 4. A Task 10 fecha.

## Se o tempo apertar

Ordem de corte, do último a cortar para o primeiro:

1. Nunca cortar: Task 1 (números), Task 3 (kit), Task 5 (deck), Task 9 (notas).
2. Cortar por último: Task 6 (skill), que é insumo da Prática 4.
3. Cortável com pendência registrada: Task 8 (material de apoio), Task 7 (notebook, se o upload no Gemini estiver confirmado funcionando).
4. Cortável para depois da aula: Task 2 (validador), Task 10 (ADRs e portal).

Se a Task 2 for cortada, a diretiva editorial passa a depender de revisão manual do deck, e isso precisa ser dito explicitamente ao professor.

## Pendências que este plano não resolve

1. Confirmar que a máquina corporativa da turma permite instalar o Antigravity, e que a conta Google corporativa aceita upload de CSV no Gemini. Verificação do professor, antes das 14h.
2. O deck da Trilha de Negócios da manhã de 15/08 não foi recebido, então o resgate da abertura amarra apenas com a Aula 01 da tarde.
